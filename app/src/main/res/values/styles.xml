<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="StyleForTextView" parent="AppTheme">
        <item name="android:textColor">@color/colorText</item>
        <item name="android:gravity">left</item>
        <item name="android:padding">10dp</item>
        <item name="android:textSize">25dp</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="StyleForCustomButton" parent="AppTheme">
        <item name="android:text">@string/name_button</item>
        <item name="android:layout_gravity">right</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <style name="StyleForSettingsTextView" parent="AppTheme">
        <item name="android:layout_marginLeft">20dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:textSize">20sp</item>
        <item name="android:layout_width">50dp</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="StyleForSettingsEditText" parent="AppTheme">
        <item name="android:layout_marginRight">20dp</item>
        <item name="android:layout_weight">2</item>
        <item name="android:layout_width">100dp</item>
        <item name="android:layout_height">wrap_content</item>
    </style>


</resources>
